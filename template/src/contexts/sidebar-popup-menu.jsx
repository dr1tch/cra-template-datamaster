import React, { createContext } from "react";
/**
 * isSidebarPopupMenuOpen
 * *  This context will be specially to verify if the sidebar popup menu is open or not
 */

const isSidebarPopupMenuOpen = createContext(null);

export default isSidebarPopupMenuOpen;
